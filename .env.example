# OpenAI API Key for poem generation
VITE_OPENAI_API_KEY=your_openai_api_key_here

# Note: In production (Netlify), set this in your deployment environment variables
# Netlify: Site settings > Build & deploy > Environment variables